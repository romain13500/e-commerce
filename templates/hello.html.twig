<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Salut</title>
</head>
<body>
    <h1>Salam Aleykoum {{prenom}}!</h1>
    <h2>Vous avez {{age}} ans </h2>

{# condition de majorité #}
    {% if age >= 18 %}
        vous etes majeur
    {% else %}
        Vous etes mineur
    {% endif %}

{# boucle #}

    <ul>
        {% for personne in prenoms %}
            <li>prenom : {{ personne | upper }}</li>
            <li>prenom : {{ personne  }}</li>
        {% endfor %}
    </ul>
        
    <ul>
        {% for i in 1..10 %}
            <li> Numero {{i}}</li>
        {% endfor %}

    </ul>
</body>
</html>